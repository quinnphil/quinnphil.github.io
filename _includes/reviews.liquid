<div class="col">
  <a href="{% if review.redirect %}{{ review.redirect }}{% else %}{{ review.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if review.img %}
        {%
          include figure.liquid
          loading="eager"
          path=review.img
          sizes = "250px"
          alt="review thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ review.title }}</h2>
        <p class="card-text">{{ review.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if review.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ review.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if review.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ review.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
